# Database
# For Neon: use the pooled connection string for DATABASE_URL
# and the direct (non-pooled) connection string for DIRECT_URL
# Both are available in your Neon dashboard under "Connection Details"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/investment_tracker"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/investment_tracker"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-in-production-use-openssl-rand-base64-32"

# Dev Auth — set to "true" locally only, never in production
# When true, any email/password automatically creates an account on first login
DEV_AUTH_ENABLED="false"
